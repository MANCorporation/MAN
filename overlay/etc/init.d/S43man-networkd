#!/bin/sh

case "${1:-}" in
    start) /usr/sbin/man-networkd >>/var/log/man-network.log 2>&1 & ;;
    stop) killall man-networkd 2>/dev/null || true ;;
    restart) "$0" stop; "$0" start ;;
    *) echo "Usage: $0 {start|stop|restart}" >&2; exit 2 ;;
esac
