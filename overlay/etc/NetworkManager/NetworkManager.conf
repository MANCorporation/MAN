[main]
plugins=keyfile
dhcp=dhcpcd
# Keep NetworkManager authoritative for every interface and retry a connection
# indefinitely after a transient UTM/virtio link or DHCP failure.
no-auto-default=*
autoconnect-retries-default=-1

[logging]
level=INFO
