# interface file auto-generated by buildroot

auto lo
iface lo inet loopback

# UTM's ARM virt machine may name its virtio NIC eth0, enp0s1, or another
# predictable interface name.  MAN's S42man-network script discovers it and
# obtains its address through DHCP instead of hard-coding a device name.
